package compose

const (
	// projectNameLabel is the label key for the project name.
	projectNameLabel = "io.mutagen.compose.project.name"
	// daemonIdentifierLabel is the label key for the daemon identifier.
	daemonIdentifierLabel = "io.mutagen.compose.daemon.identifier"
)
