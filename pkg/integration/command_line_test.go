package integration

// TODO: Implement end-to-end tests that operate via the Mutagen CLI. We can use
// the CLI that resides in the build directory (which will also already have an
// agent bundle next to it).
