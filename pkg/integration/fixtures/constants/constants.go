package constants

const (
	// HTTPDemoResponse is the response that should be returned from the HTTP
	// demo server.
	HTTPDemoResponse = "Hello from the demo"
	// HTTPDemoBindAddress is the bind address for the HTTP demo server.
	HTTPDemoBindAddress = "localhost:8080"
)
