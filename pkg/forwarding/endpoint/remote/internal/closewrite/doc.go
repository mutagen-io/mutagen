// Package closewrite implements a simple framing protocol on top of connections
// to facilitate write closure (i.e. a CloseWrite method) for connection
// implementations that don't provide it natively.
package closewrite
