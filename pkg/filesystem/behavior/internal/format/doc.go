// Package format provides filesystem format enumerations and querying
// functions. It is designed to support the behavior package, so it only detects
// a limited number of well-known filesystems on a limited number of platforms,
// and thus it's probably not generally useful.
package format
