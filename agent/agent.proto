syntax = "proto3";

package agent;

message PromptRequest {
    string prompter = 1;
    string prompt = 2;
    string context = 3;
}

message PromptResponse {
    string response = 1;
}

service Prompt {
    rpc Prompt(PromptRequest) returns (PromptResponse) {}
}
