syntax = "proto3";

package agent;

message PromptRequest {
    string prompter = 1;
    string context = 2;
    string prompt = 3;
}

message PromptResponse {
    string response = 1;
}

service Prompt {
    rpc Prompt(PromptRequest) returns (PromptResponse) {}
}
